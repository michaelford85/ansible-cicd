---
- name: Update yum packages on Linux
  hosts: all
  gather_facts: yes

  tasks:

    - name: upgrade all packages
      yum:
        name: '*'
        state: latest
      become: yes
      when: tags.demo == "cicd"

    - name: install packages
      yum:
        name: "{{ packages }}"
      vars:
        packages:
        - wget
        - tar
        - python3-pip
      become: yes
      when: tags.demo == "cicd"
      
